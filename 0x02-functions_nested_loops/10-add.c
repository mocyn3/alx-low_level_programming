#include "main.h"

/**
 * add - adds two integers and returns the results
 * @a: int added to b
 * @b: int added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
