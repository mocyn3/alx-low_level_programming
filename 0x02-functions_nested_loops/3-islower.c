#include "main.h"

/**
 * _islower - checks for lowercase character
 * @a: the character to check
 * Return: 1 if a is lowercase, 0 otherwise
 */
int _islower(int a)
{
	return (a >= 'a' && a <= 'z');
}
